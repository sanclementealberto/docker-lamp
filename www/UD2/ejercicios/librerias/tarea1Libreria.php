<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php include("../funciones/tarea1Funciones.php");
    ?>

    <?php
        /**  include() incluye e interpreta el contenido del archivo
         * las librerias nos permite agrupar varias funciones y variables
         * en un mismo archivo asi podemos incluir la libreria en diferentes paginas
        */

        /**
         * require() hace lo mismo que include() pero produce un error falta no caso de fallo
         */
        /**
         * include_once() igual que include() con la diferencia que ya se incluyo el archivo anteriormente
         * no se vuelve a incluir solo una vez
         */

         /**
          * require_once igual que require() con la diferencia que ya se incluyo en el archivo anteriormente
          * no se vuelve a incluir
          */
    ?>
    
</body>
</html>